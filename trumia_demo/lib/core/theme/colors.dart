import 'package:flutter/material.dart';

class TrumiaColors {
  TrumiaColors._();

  static const bgPrimary = Color(0xFFF1F2F4);
  static const surfaceBase = Color(0xFFFAFAFB);
  static const surfaceElevated = Color(0xFFFFFFFF);
  static const surfaceMuted = Color(0xFFEDEEF1);

  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF8E8E93);
  static const textTertiary = Color(0xFFB0B0B5);

  static const accentGreen = Color(0xFF1FBF8E);
  static const accentGreenDark = Color(0xFF18A176);
  static const accentGreenSoft = Color(0x331FBF8E);

  static const cardTeal = Color(0xFF4A6B6E);
  static const cardTealDark = Color(0xFF2E4548);

  static const dimOverlay = Color(0x66000000);
  static const hairline = Color(0x14000000);
}
